node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.venv/
scraper/weather_data/

# Local dev fixtures (per-station data); committed sample keeps only singletons
static/sample/history/
static/sample/20*/

# Core data views downloaded at build time by scripts/bake-data.mjs
static/baked/

# Per-city OG share images generated at build time by scripts/build-og.mjs
static/og/

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Raw Natural Earth sources for scripts/build-map-data.sh — not committed.
# Re-download from https://www.naturalearthdata.com/downloads/ to rebuild
# static/data/*.json; the generated JSON is what the app ships.
src/lib/assets/ne_*
